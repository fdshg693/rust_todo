## Status: completed

以下のスクリプトを必要に応じて呼び出すエージェントを作成してください:
ビルド・アプリケーションの起動・テスト・デプロイなどの複数プロセスが同時に起動する必要がある場合、`scripts\procman.py`を使う。
ユーザからの応答を求めるときは、`scripts\echo_input.py`を使う。
HTTPリクエストを使って、ローカルアプリをテストする際は、`scripts\http_request.py`を使う。

---
**Completed:** Created `.github/agents/script.runner.agent.md`